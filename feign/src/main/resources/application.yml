spring:
  application:
    name: FEIGN-SERVICE

server:
  servlet:
    context-path: /feign
  port: 0

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance: localhost

